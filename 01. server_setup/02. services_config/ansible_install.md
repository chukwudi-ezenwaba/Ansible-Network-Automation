In this lab, we would install ansible on the control node Fedora1.
1. Update packages to the latest version.
    ```bash
    sudo dnf update -y

2. Install anible
    ```bash
    sudo dnf install ansible -y 

3. Chech ansible version
    ```bash
    ansible --version
